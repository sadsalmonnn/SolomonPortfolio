import { ScrollInViewDirective } from './scroll-in-view.directive';

describe('ScrollInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollInViewDirective();
    expect(directive).toBeTruthy();
  });
});
